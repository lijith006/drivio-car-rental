class Booking {
  String name;
  DateTime startDate;
  DateTime endDate;
  String location;
  double totalPrice;

  Booking({
    required this.name,
    required this.startDate,
    required this.endDate,
    required this.location,
    required this.totalPrice,
  });
}
